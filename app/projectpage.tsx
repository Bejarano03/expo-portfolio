import { View, Text } from 'react-native'

export default function ProjectPage() {
    return (
        <View>
            <Text>Edit /app/projectpage.tsx to make changes to the page</Text>
        </View>
    );
}